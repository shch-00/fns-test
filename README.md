# Vacancy Application Manager

## Описание проекта
Приложение для создания, отображения и редактирования заявок на размещение вакансий. Реализовано с использованием React и Formik для удобной работы с формами, а также Yup для валидации.

## Функциональность
- 📌 **Создание заявки** — форма с валидацией.
- 📌 **Отображение списка заявок** — загруженные данные отображаются в виде списка.
- 📌 **Редактирование заявки** — возможность обновлять данные существующей заявки.
- 📌 **Валидация форм** — ошибки отображаются под соответствующими полями.
- 🎨 **Опционально**: стилизация селекта и DatePicker.

## Технический стек
- ⚛ **React + Hooks**
- 📜 **Formik + Yup** (для валидации форм)
- 📂 **Mock JSON / аналог** (хранение данных)

## Установка и запуск
```bash
# 1. Клонируем репозиторий
git clone https://github.com/shch-00/fns-test.git
cd fns-test

# 2. Устанавливаем зависимости
npm install

# 3. Запускаем локальный сервер
npm start
```

## Использование

### (Локально)
1. Перейдите в браузер по адресу, сформированному Vite, например `http://localhost:5173`.
2. Добавьте новую заявку через форму.
3. Посмотрите список заявок.
4. Нажмите «Редактировать», чтобы изменить заявку.

### (Глобально)
1. Перейдите в браузер по адресу https://shch-00.github.io/fns-test/
2. Добавьте новую заявку через форму.
3. Посмотрите список заявок.
4. Нажмите «Редактировать», чтобы изменить заявку.

## Деплой
Готовый проект доступен по ссылке: [деплой](https://shch-00.github.io/fns-test/).

___
## **Используемые библиотеки в проекте**

### **Основные зависимости (`dependencies`):**

✅ **UI & React-экосистема:**

- **`react` / `react-dom`** — React 19  
- **`react-router-dom`** — маршрутизация (React Router 7)  
- **`react-select`** — кастомный селект  
- **`react-svg`** — работа с SVG в React  

✅ **Формы и валидация:**

- **`formik`** — управление формами  
- **`yup`** — валидация форм  
- **`react-datepicker`** — календарь для выбора дат  
- **`@types/react-datepicker`** — типы для `react-datepicker`  

✅ **Дата и время:**

- **`date-fns`** — работа с датами  

✅ **Работа с API:**

- **`@tanstack/react-query`** — управление серверными запросами  
- **`uuid`** — генерация уникальных идентификаторов

---

### **Dev-зависимости (`devDependencies`):**

✅ **Сборка и разработка:**

- **`vite`** — быстрая сборка  
- **`@vitejs/plugin-react`** — поддержка React в Vite  
- **`typescript`** — TypeScript  
- **`json-server`** — моковый сервер API
